import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Team',
  description:
    'Team page',
};

const pageTeam = () => {
  return (
    <div>pageTeam</div>
  )
}

export default pageTeam