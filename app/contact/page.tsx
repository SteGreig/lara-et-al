import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contact',
  description:
    'Contact page',
};

const pageContact = () => {
  return (
    <div>pageContact</div>
  )
}

export default pageContact